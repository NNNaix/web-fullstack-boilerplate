import { CustomWindow } from '@app/typings/type';

export const DATE_FORMAT = 'YYYY-MM-DD HH:mm:ss';
export const DEFAULT_PAGE_SIZE = 20;
export const globalConfig = (window as CustomWindow).dataLayer;
